# Backups and dev artifacts — not part of the deployed site
*.bak
*.bak-*
*.bak2
og-image-source.html
og-image-source-backup-sunset-coral.html
WELLY_EVENT_SCAN_PROMPT.md

# Local Netlify CLI state
.netlify/

# Geocoding cache (dev artifact; coords are merged into venues.json)
geocode_cache.json

# Venue outreach working files — contain scraped business contact details.
# This repo's publish dir is the repo ROOT, so anything committed here is publicly
# fetchable at welly-deals-by-day.co.nz/<file>. These must never be committed.
woap_outreach.json
WOAP_VENUE_OUTREACH.md
